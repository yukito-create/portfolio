import Preparation from "../components/preparation";

export default function BLOG() {
  const preparation = {
    fontSize: "3rem",
    fontWeight: "bold",
    minHeight: "100vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  };

  return (
    <>
      <Preparation />
    </>
  );
}